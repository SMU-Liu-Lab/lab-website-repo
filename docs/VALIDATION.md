# 交付检查记录

日期：2026-09-16。

## 已完成

- JavaScript 语法检查：`content.js`、`main.js`、`preview.mjs` 均通过 Node 语法检查。
- 静态文件与数据检查：6 个应用地址与用户原始路径逐项一致（保留 HLA-disease、RareDipper 大小写）；8 个唯一 PMID；成员数组为空；HTML 及数据引用的 5 个本地资源文件都存在。
- 本地 HTTP 服务：入口 HTML、CSS、两个 JS 和两张图片均 HTTP 200，MIME 类型正确。
- 浏览器实际渲染：显示 6 张工具卡片、8 篇论文和学校官网真实导师照片；所有页内锚点都有对应目标。
- 桌面 1440px 宽度：首页与平台直达布局已截图检查，无横向溢出。发现并修复 CSS 自定义变量图片 URL 解析重复目录的问题，修复后已确认背景图正确显示。
- 手机 390px 宽度：单列卡片、两列平台快捷链接、折叠菜单；未检测到页面横向溢出。截图检查了首页和导师板块。
- 手机 320px 宽度：论文按单列显示，无横向溢出，长英文标题正常换行，8 篇全部存在。
- 菜单操作：打开后 `aria-expanded=true` 且导航可见；选择“导师介绍”和“学术论文”后正确定位并关闭菜单。预览尺寸已恢复。
- 外部平台连通性：六个 HTTPS URL 均 HTTP 200；MedReview/DrugReview 页面文字与用户简介一致。进一步加载 CHIP Search 的真实页面，确认其为 CHIP 风险查询与森林图，并修正清单中 ChIP-seq 的误述。
- 浏览器检查时未记录到页面 JavaScript 警告/错误；HTTP 与图片资源另行检查。
- 导师与论文证据：见 SOURCES.md，论文采用 NCBI Entrez 逐篇作者字段核验。

## 尚未执行／检查边界

- 没有登录阿里云服务器，也没有上传、替换首页、reload Nginx 或改动现有六个应用。
- Nginx 片段尚未与服务器实际配置合并，因此尚未运行服务器端 `nginx -t` 或线上回归。
- 外部科研工具只检查连接和页面，未运行科学分析、验证其风险数值、提交材料或检查登录后的流程。
- Virtual Cell、HLA Disease Database 与 RareDipper 简介主要依据用户提供清单，未完成其加载后业务界面核验。
- 未做完整无障碍审计、真实手机硬件测试或 200% 文本缩放专项验证。

本记录区分已完成的本地检查与上线后需要执行的检查，不代表服务器已经部署。

## 8507 部署方式更新

按用户后续要求，增加 `server.mjs` 生产入口、systemd 用户服务模板和 8507 的 Nginx 反代片段；README 与部署文档已同步更新。旧页面视觉和内容没有修改，复用上述有效验证结果。

- `npm run check`：生产服务及网页 JS 语法检查通过。
- `npm test`：5 项通过，覆盖首页和五个静态资源、HEAD 无正文且元数据正确、仓库文件/目录/路径穿越不泄露、非法 URL/不支持的方法处理、环回监听与查询参数。
- 通过子进程实际运行默认 `node server.mjs`，在本机 `127.0.0.1:8507` 验证首页 200 和 CSS HEAD 响应，随后仅停止本次测试子进程。
- 尚未在阿里云执行 systemd 安装、linger 检查或 `nginx -t`；这些由 OpenClaw 根据仓库提示词在目标服务器执行。
